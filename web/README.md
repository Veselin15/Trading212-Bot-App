## Web portal (Next.js)

This is the user portal for authentication, subscription management, license keys, and desktop downloads.

### Run locally

```bash
cd web
npm install
npm run dev
```

### Environment variables

See `ENV.md`.
